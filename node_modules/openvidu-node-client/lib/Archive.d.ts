export declare class Archive {
    private id;
    private name;
    private sessionId;
    private createdAt;
    private size;
    private duration;
    private url;
    private hasaudio;
    private hasvideo;
    private status;
    constructor(json: JSON);
    getStatus(): Archive.Status;
    getId(): string;
    getName(): string;
    getSessionId(): string;
    getCreatedAt(): number;
    getSize(): number;
    getDuration(): number;
    getUrl(): string;
    hasAudio(): boolean;
    hasVideo(): boolean;
}
export declare namespace Archive {
    enum Status {
        starting = 0,
        started = 1,
        stopped = 2,
        available = 3,
        failed = 4,
    }
}
